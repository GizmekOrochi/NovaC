Type system example
===================

This example defines two primitive types, an implicit conversion and one typed
addition overload. NovaC resolves the operation and returns the conversion plan.

.. literalinclude:: ../../examples/type_system.cpp
   :language: cpp
   :linenos:
